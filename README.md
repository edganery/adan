# adan

## to get started

- on mac install `brew` at [brew.sh](https://brew.sh)
- `brew install meteor`
- `git clone` this repo
- `cd` into the repo directory
- then execute
```
npm install
meteor run
```
